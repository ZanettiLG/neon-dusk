/** App footer tagline (port of AppFooter.vue). */
export default function AppFooter() {
  return (
    <footer className="bg-nd-surface border-t border-nd-cyan/10 px-4 py-3">
      <p className="text-nd-text-secondary text-xs text-center font-data">
        Monta teu cromo. Queima teu nome. Vira lenda.
      </p>
    </footer>
  );
}
